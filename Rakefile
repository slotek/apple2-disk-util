require 'rake/testtask'

desc "Run the integration test"
task :it do
  #TODO
  #Dir.chdir('it') { system "test.sh" }
end

Rake::TestTask.new do |t|
  t.warning = true
  t.verbose = true
end

task :default => :test
